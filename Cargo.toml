[package]
name = "rcontext"
version = "0.1.0"
edition = "2024"
description = "Merge codebase into context or count lines"
license = "MIT"

[dependencies]
clap = { version = "4.0", features = ["derive"] }
ignore = "0.4"
rayon = "1.11.0"
